# Models

Put trained model checkpoints here locally.

Expected example:

```text
models/best_safari_wavlm_ensemble.pt
```

Model checkpoint files are not committed to Git because they are large binary artifacts.

Ignored extensions include:

```text
*.pt
*.pth
*.ckpt
*.onnx
```

